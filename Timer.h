#ifndef TIMER_H
#define TIMER_H

/**
 * A timer will interrupt the processor after every X instruction,
 * where X is a command-line parameter.
 *
 * @author
 */
class Timer
{
    public:
        Timer();
        virtual ~Timer();

    protected:

    private:
};

#endif // TIMER_H
